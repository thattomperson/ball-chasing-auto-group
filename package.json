{
  "name": "chasing-sessions",
  "private": true,
  "scripts": {
    "build": "mkdir -p public; touch public/index.html"
  },
  "dependencies": {
    "@now/node": "^1.3.5",
    "node-fetch": "^2.6.0"
  }
}
